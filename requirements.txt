# pip install -r requirements.txt - to install Python 3rd parties
# It is recommended to do it in virtual environment and launch servers there:
# Ubuntu:
# - python3 -m venv venv-linux   - !!! one time only - to install venv !!!
# - source venv-linux/bin/activate
# - pip install -r requirements.txt
# Win/PowerShell:
# - python -m venv venv-win   - !!! one time only - to install venv !!!
# - .\venv-win\Scripts\Activate.ps1
# - pip install -r requirements.txt
#

# Core dependencies - use minimum versions with compatibility
fastapi>=0.100.0
pydantic>=2.0.0
SQLAlchemy>=2.0.0
uvicorn>=0.20.0

# Form data support (for file uploads)
python-multipart>=0.0.6

# Testing dependencies
pytest>=8.0.0
httpx>=0.25.0  # Required for FastAPI TestClient

# Authentication
python-jose[cryptography]>=3.3.0  # JWT encode/decode
bcrypt>=4.0.0                      # password hashing

# Email
aiosmtplib>=3.0.0                  # async SMTP for invitation/approval emails

# Local desktop installer runtime (NOVOTREE_APP_MODE=local).
# Web/VM deployment never imports these — they are needed only by
# backend/local_launcher.py, which is the entrypoint for the packaged app.
pywebview>=5.0                     # native window wrapping the SPA (WebView2 on Windows)
platformdirs>=4.0                  # cross-platform user_data_dir / user_config_dir

# Transitive dependencies (installed automatically, listed for reference)
# starlette, anyio, typing_extensions, etc. are pulled by fastapi/pydantic
